# ============================================================
# Secret — Database credentials
# In production (EKS/bare-metal), replace stringData values
# with references from AWS Secrets Manager or Vault.
# For local/dev use, fill values directly.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: java-monolith-secret
  namespace: java-monolith
  labels:
    app.kubernetes.io/name: java-monolith
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "changeme"
  MYSQL_DATABASE: "IbtisamIQbankappdb"
  MYSQL_USER: "appuser"
  MYSQL_PASSWORD: "changeme"
  SPRING_DATASOURCE_USERNAME: "appuser"
  SPRING_DATASOURCE_PASSWORD: "changeme"
