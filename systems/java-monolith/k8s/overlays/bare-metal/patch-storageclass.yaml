# k8s/overlays/bare-metal/patch-storageclass.yaml
# Patches mysql-pvc to use local-path provisioner for bare-metal clusters.

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: bankapp
spec:
  storageClassName: local-path
